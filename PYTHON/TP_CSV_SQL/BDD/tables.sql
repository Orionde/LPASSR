CREATE TABLE fonctions (
    name varchar(64)
);

CREATE TABLE department(
    name varchar(64)
);

CREATE TABLE users (
    nom varchar(64), 
    prenom varchar(64),
    login varchar(64),
    passwd varchar(64),
    email varchar(64)
);

